# 2026-07-15 耗时测试记录

## 测试目的

验证当前 DepthPro + 3D scaffold + 支撑平面体积计算在云端部署前的基础运行情况。

## 本次本地环境结果

本次没有得到有效算法耗时，原因是当前 Python 环境依赖不完整：

```text
ModuleNotFoundError: No module named 'cv2'
ModuleNotFoundError: No module named 'scipy'
ModuleNotFoundError: No module named 'timm'
```

已确认当前环境中 `torch` 存在，但版本为 CPU 版：

```text
torch 2.4.1+cpu
```

因此本机这次不适合作为 10 秒推理目标的性能基准。

## 后续基准测试要求

需要先准备统一后端环境：

```powershell
pip install -r requirements.txt
```

如果云端有 CUDA GPU，还需要安装匹配 CUDA 版本的 PyTorch。正式计时应拆分记录：

```text
auto_mask_seconds
depthpro_seconds
scaffold_seconds
volume_seconds
total_seconds
```

本周目标是热启动状态下 `total_seconds <= 10`。
